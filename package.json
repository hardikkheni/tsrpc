{
  "name": "jsontpc-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "typecheck": "turbo run typecheck",
    "lint": "biome check packages/",
    "lint:fix": "biome check --write packages/",
    "changeset": "changeset",
    "release": "changeset publish",
    "version": "changeset version && pnpm lint:fix",
    "build:publish": "turbo run build && changeset publish",
    "check:publint": "publint packages/core && publint packages/http && publint packages/tcp && publint packages/ws && publint packages/express && publint packages/fastify && publint packages/nestjs",
    "check:attw": "attw --pack packages/core && attw --pack packages/http && attw --pack packages/tcp && attw --pack packages/ws && attw --pack packages/express && attw --pack packages/fastify && attw --pack packages/nestjs",
    "postinstall": "lefthook install"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.17.0",
    "@biomejs/biome": "^1.9.0",
    "@changesets/cli": "^2.27.0",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "lefthook": "^1.7.0",
    "publint": "^0.3.0",
    "tsup": "^8.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0"
  }
}
